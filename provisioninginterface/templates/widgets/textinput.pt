<span tal:omit-tag="">
    <input type="text" name="${field.name}" value="${cstruct}"
           tal:attributes="size field.widget.size;
                           class field.widget.css_class"
           id="${field.oid}"/>
    <span id="default_text-${field.oid}" style="display: none;">${field.schema.default}</span>

    <script tal:condition="field.widget.mask" type="text/javascript">
        deform.addCallback(
                '${field.oid}',
                function (oid) {
                    $("#" + oid).mask("${field.widget.mask}",
                            {placeholder:"${field.widget.mask_placeholder}"});
                });
    </script>
	<script tal:condition="field.schema.default" type="text/javascript">
        submitClearsDefaultValues("${field.oid}", "${field.schema.default}");

        deform.addCallback(
                '${field.oid}',
                function (oid) {
                    var default_text = document.getElementById("default_text-${field.oid}").innerHTML;
                    prepareTextField("${field.oid}", default_text);
                });
    </script>

</span>
